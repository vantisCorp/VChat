# Run pre-push checks
echo "Running pre-push checks..."

# Run linting
pnpm run lint 2>/dev/null || echo "No lint script available"

# Check for vulnerabilities
pnpm audit --audit-level=moderate 2>/dev/null || true